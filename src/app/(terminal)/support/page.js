"use client";

/**
 * /support — FAQ + тикеты + эксперт-чат
 */

import SupportCenter from "@/components/terminal/SupportCenter";

export default function SupportPage() {
  return <SupportCenter />;
}
